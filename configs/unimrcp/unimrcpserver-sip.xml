<?xml version="1.0"?>
<unimrcpserver>
  <!-- UniMRCP Server Template (MRCPv2 over SIP) -->
  <sip-ua>
    <local-ip>0.0.0.0</local-ip>
    <local-port>5060</local-port>
    <transport>udp</transport> <!-- udp | tcp | tls -->
    <user>unimrcp</user>
    <realm>example.local</realm>
    <!-- 필요 시 registrar/proxy 설정 추가
    <registrar>sip:proxy.example.local</registrar>
    <outbound-proxy>sip:proxy.example.local:5060;transport=udp</outbound-proxy>
    -->
  </sip-ua>

  <rtp-settings>
    <ip>0.0.0.0</ip>
    <rtp-port-min>40000</rtp-port-min>
    <rtp-port-max>40100</rtp-port-max>
    <ptime>20</ptime>
  </rtp-settings>

  <plugins>
    <plugin>demo-recog</plugin>
    <plugin>demo-synth</plugin>
  </plugins>

  <!--
    검증 체크리스트
    - SIP 포트/트랜스포트: 5060/udp(환경에 맞게 변경)
    - RTP 포트 범위: 40000-40100 (방화벽 허용)
    - 리소스: recog/synth 플러그인 로드 확인
    - 코덱: PCMU(PT=0) 또는 L16(동적 PT) 매칭 — 앱의 STT_ENCODING, STT_RATE와 일치 필요
    - 에코/Mock: demo-recog 사용 시 간단 결과가 반환됨
  -->
</unimrcpserver>
