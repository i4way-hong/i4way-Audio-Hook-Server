# 다음 진행 사항 (업데이트: 2025-11-14)

**Hardening 단계(Phase 0)가 성공적으로 마무리되었습니다.** 이 문서는 제품 로드맵의 다음 단계인 **MVP Beta(Phase 1)를 향한 구체적인 실행 계획**을 정의합니다.

## 완료된 작업 (Phase 0: Hardening)

- **[완료] 설정 검증 강화**: 잘못된 설정 값에 대한 명확한 에러 메시지와 안전한 폴백 로직을 구현했습니다.
- **[완료] TCP 안정성 확보**: Half-close 및 Back-pressure 시나리오에 대한 처리 로직을 보강하고 통합 테스트를 통해 안정성을 검증했습니다.
- **[완료] 리샘플링 기반 마련**: 8k↔16k 리샘플링 단위 테스트와 기본 성능 벤치마크를 구축했습니다.
- **[완료] STT 프로토콜 추상화**: WebSocket, TCP, gRPC, MRCP 포워더의 기본 구조를 구현하고, 프로파일 기반의 핸드셰이크 아키텍처를 도입했습니다.
- **[완료] 문서 및 환경변수 정리**: README와 .env 예제를 최신화하여 개발 환경 설정을 간소화했습니다.
- **[완료] 로깅 시스템 강화**: 로그 회전 및 보존 정책을 구현하고 테스트를 통해 안정성을 확인했습니다.

---

## 다음 스프린트 계획 (Phase 1: MVP Beta 준비)

**목표**: 제품화 가능한 빌드를 출시하기 위한 핵심 기능과 운영 기반을 마련합니다.

### 스프린트 1: gRPC 및 운영 기반 강화

1.  **gRPC 포워더 GA 수준 완성**
    - **작업**: 현재 베타 수준인 gRPC 포워더에 TLS 인증서 관리, 재연결 정책 고도화(Jitter 적용), 메타데이터(특히 `conversation_lookup`) 전송 방식을 표준화합니다. 벤더사양에 맞춘 최종 proto 메시지 포맷을 확정하고 관련 테스트를 보강합니다.
    - **수락 기준**:
        - TLS 연결 및 인증서 기반 인증이 안정적으로 동작합니다.
        - 스트림 단절 시, 설정된 백오프 및 Jitter 전략에 따라 재연결을 시도하고 성공합니다.
        - `StreamingRecognize` 요청 시 전송되는 `SessionInit` 메시지에 `conversation_lookup` 결과가 포함되었음을 테스트 서버에서 검증할 수 있습니다.

2.  **컨테이너 기반 배포 파이프라인 구축 (Docker & Helm)**
    - **작업**: 멀티 스테이지 Dockerfile을 최적화하여 경량의 프로덕션 이미지를 생성합니다. 기본적인 Helm Chart를 작성하여 Kubernetes 환경에 쉽게 배포할 수 있도록 지원합니다. (ConfigMap, Secret, Service 포함)
    - **수락 기준**:
        - `docker build`를 통해 프로덕션용 이미지가 성공적으로 빌드됩니다.
        - `helm install` 명령어로 EKS/GKE 등 표준 K8s 환경에 배포되고, 서비스가 정상적으로 트래픽을 수신합니다.

3.  **비밀 관리 체계 연동 (Secrets Manager)**
    - **작업**: `secrets-plugin.ts`를 활용하여 AWS Secrets Manager 또는 HashiCorp Vault와 같은 외부 시스템에서 STT 인증키, API 키 등의 비밀 정보를 동적으로 가져오도록 구현합니다.
    - **수락 기준**:
        - `.env` 파일에 민감한 정보를 직접 하드코딩하지 않고도 서비스가 시작되고 STT 연동에 성공합니다.
        - 비밀 정보 조회 실패 시, 명확한 에러 로그를 남기고 서비스 시작이 실패 처리됩니다.

### 스프린트 2: 멀티 벤더 및 테스트 자동화

1.  **핸드셰이크 프로파일 확장 (멀티 벤더 지원)**
    - **작업**: 2개 이상의 주요 STT 벤더(예: Google, Microsoft)의 핸드셰이크 및 메시지 포맷을 지원하는 프로파일을 추가로 구현합니다. `SttVendorPlugin` 인터페이스를 활용하여 벤더별 로직을 명확히 분리합니다.
    - **수락 기준**:
        - `.env` 설정 변경만으로 타겟 STT 벤더를 교체하고, 각 벤더의 에코 서버와 정상적으로 통신합니다.
        - 각 벤더 프로파일에 대한 통합 테스트 케이스가 추가되고 CI에서 통과됩니다.

2.  **자동 회귀 테스트 매트릭스 구축**
    - **작업**: (프로토콜 × 코덱 × 리샘플링 여부) 조합의 E2E 테스트 시나리오를 자동화합니다. GitHub Actions 등 CI 환경에서 PR 제출 시 자동으로 실행되도록 파이프라인을 구성합니다.
    - **수락 기준**:
        - 주요 기능 변경 시, 핵심 기능 조합에 대한 회귀 테스트가 자동으로 수행됩니다.
        - 테스트 실패 시 PR이 머지되지 않도록 CI에 품질 게이트를 설정합니다.

3.  **관측성 강화 (대시보드 및 경보)**
    - **작업**: 현재 수집 중인 Prometheus 지표(`active_sessions`, `forwarder_latency` 등)를 기반으로 기본 Grafana 대시보드를 제작합니다. 세션 생성 실패, STT 재연결 반복 등 주요 장애 상황에 대한 경보(Alert) 규칙을 정의합니다.
    - **수락 기준**:
        - Grafana 대시보드에서 실시간 세션 수와 STT 포워더의 지연 시간을 시각적으로 확인할 수 있습니다.
        - 특정 임계치(예: 5분 내 10회 이상 재연결) 초과 시 경보가 발생함을 모의 테스트로 검증합니다.

---
*이전 계획은 `PRODUCT_PLAN.md`의 로드맵으로 통합 관리합니다.*
